import SwiftUI

// Additional layout groupings (could merge with AppSpacing later if desired)
public enum LayoutMetrics {
    public static let vStackDetailSpacing: CGFloat = 16
    public static let detailTopPadding: CGFloat = 12
    public static let detailBottomPadding: CGFloat = 40
    public static let backButtonSpacing: CGFloat = 4
}
